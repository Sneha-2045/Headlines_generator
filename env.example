# Copy this file to .env and set values for your environment.
# Server (Express)
PORT=3001

# Client (React) – only needed when API is on a different origin.
# Leave unset or use /api when frontend is served with proxy (e.g. Vite dev, same host in prod).
# VITE_API_URL=/api
# VITE_API_URL=https://api.yoursite.com
